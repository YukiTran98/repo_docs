# rag-practice — Function Reference

> Generated by Repo Analyzer · April 12, 2026
> 2 functions mapped across 1 core file

---

## Table of Contents

- [rag.py](#ragpy)
  - [rag](#rag)
  - [get_embedding](#get_embedding)

---

## `rag.py`

> Core RAG layer — chứa toàn bộ logic tìm kiếm vector: embedding query, query ChromaDB, format kết quả trả về cho LLM agent.

---

### `rag` [ENTRY POINT] [PUBLIC] [SIDE EFFECTS]

**Type:** `function`
**Importance:** 100/100 `██████████`

```python
@function_tool
def rag(query: str) -> str
```

Tool function chính được Agent gọi để tìm kiếm thông tin sản phẩm. Nhận query text, convert sang embedding, query ChromaDB lấy top 10 kết quả gần nhất, format metadata thành chuỗi trả về cho LLM tổng hợp.

Được đánh dấu `@function_tool` nên tự động được expose cho OpenAI Agents SDK — LLM thấy schema của function và tự gọi khi cần. Kết quả trả về là raw text context, LLM chịu trách nhiệm tổng hợp thành câu trả lời.

**Used in:** `rag_agent.py` (tools=[rag])

---

### `get_embedding` [SIDE EFFECTS]

**Type:** `function`
**Importance:** 50/100 `█████░░░░░`

```python
def get_embedding(text: str) -> list[float]
```

Helper function convert text thành dense vector embedding qua OpenAI API (`text-embedding-3-small`). Trả về raw embedding list — caller (`rag()`) tự normalize trước khi dùng.

Không được export ra ngoài, chỉ dùng nội bộ trong `rag.py`. Side effect: gọi OpenAI Embeddings API tốn token và latency.

**Used in:** `rag.py:38` (trong body của `rag()`)

---
